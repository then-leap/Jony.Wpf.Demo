<Window x:Class="Jony.Demo.ExportExcel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Jony.Demo.ExportExcel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainPageViewModel />
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <TextBlock Text="报表数据预览" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>

            <DataGrid ItemsSource="{Binding ReportData}"
                      AutoGenerateColumns="True"
                      Height="300"
                      Margin="0 0 0 20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="10" Content="选择模板" Command="{Binding SelectTemplateCommand}" />
                <Button Margin="10" Content="导出" Command="{Binding ExportCommand}" />
                <Button Margin="10" Content="打开webview2" Command="{Binding EnterBrowerCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
